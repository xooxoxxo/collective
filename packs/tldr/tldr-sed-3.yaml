id: tldr-sed-3
title: 'sed: Replace all `apple` (extended `regex`) occurrences with `APPLE` (extended
  `regex`) in all input lines and print the result to `stdout`'
cmd: <command> | sed -E 's/(apple)/\U\1/g'
platform:
- macos
domains:
- tldr-import
danger: low
explanation: Edit text in a scriptable manner
source: https://github.com/tldr-pages/tldr/blob/main/pages/osx/sed.md (CC-BY-4.0)
tags:
- sed
