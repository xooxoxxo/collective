id: tldr-awk-6
title: 'awk: Print different values based on conditions'
cmd: awk '{if ($1 == "foo") print "Exact match foo"; else if ($1 ~ "bar") print "Partial
  match bar"; else print "Baz"}' <path/to/file>
platform:
- macos
domains:
- tldr-import
danger: low
explanation: A versatile programming language for working on files
source: https://github.com/tldr-pages/tldr/blob/main/pages/common/awk.md (CC-BY-4.0)
tags:
- awk
